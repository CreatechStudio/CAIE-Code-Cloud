{
  "postCreateCommand": "echo 'export PATH=/workspaces/$(basename $GITHUB_REPOSITORY)/CAIE_Code/bin:$PATH' >> $HOME/.bashrc; git submodule update --init --recursive",
  "customizations": {
    "vscode": {
      "extensions": [
        "CreatechStudioShanghaiInc.cpc-interpreter-extension"
      ]
    },
    "codespaces": {
      "openFiles": [
        "README.md"
      ]
    }
  }
}
